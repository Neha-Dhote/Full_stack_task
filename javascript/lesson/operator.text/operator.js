<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Arithamatic Operation in JavaScript</title>
</head>

<body>

    <script>
        //create two variables
        let a = 10;
        let b = 5;

        // perform arithmatic operations
        let sum = a + b;
        let differance = a - b;
        let product = a * b;
        let quotient = a / b;
        let percentage = (a / b) * 100;

        //Display the result in the console

        console.log("a=" + a + ",b=" + b);
        console.log("Addition(a+b)=" + sum);

        console.log("Substraction(a-b)=" + differance);

        console.log("Multiplication(a*b)=" + product);

        console.log("Division(a/b)=" + quotient);

        console.log("Percentage(a is what % of b
        ) = "+percentage+" % ");

    </script>
</body>

</html>